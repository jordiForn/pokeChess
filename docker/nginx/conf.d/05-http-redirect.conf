server {
    listen 80 default_server;
    server_name
        pokechess.local
        www.pokechess.local
        api.pokechess.local
        db.pokechess.local;

    return 301 https://$host$request_uri;
}
