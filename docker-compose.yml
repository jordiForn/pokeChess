name: pokechess

services:
  dns:
    build:
      context: ./docker/bind9
      dockerfile: Dockerfile
    container_name: pokechess-dns
    restart: unless-stopped
    ports:
      - "1053:53/udp"
      - "1053:53/tcp"
    volumes:
      - ./docker/bind9/zones:/etc/bind/zones:ro
    networks:
      pokechess_net:
        ipv4_address: 10.1.0.2


  mariadb:
    image: mariadb:11.4
    container_name: pokechess-mariadb
    restart: unless-stopped
    environment:
      MARIADB_ROOT_PASSWORD: ${MARIADB_ROOT_PASSWORD:-root_secret}
      MARIADB_DATABASE: ${MARIADB_DATABASE:-pokechess}
      MARIADB_USER: ${MARIADB_USER:-pokechess}
      MARIADB_PASSWORD: ${MARIADB_PASSWORD:-pokechess_secret}
    volumes:
      - mariadb_data:/var/lib/mysql
    networks:
      pokechess_net:
        ipv4_address: 10.1.0.5


  php-fpm:
    build:
      context: .
      dockerfile: docker/php-fpm/Dockerfile
    container_name: pokechess-php-fpm
    restart: unless-stopped
    env_file:
      - .env.docker
    volumes:
      - ./backend:/var/www/backend
    depends_on:
      mariadb:
        condition: service_healthy
    networks:
      pokechess_net:
        ipv4_address: 10.1.0.4


  phpmyadmin:
    image: phpmyadmin:5-apache
    container_name: pokechess-phpmyadmin
    restart: unless-stopped
    environment:
      PMA_HOST: mariadb
      PMA_PORT: 3306
      PMA_USER: ${MARIADB_USER:-pokechess}
      PMA_PASSWORD: ${MARIADB_PASSWORD:-pokechess_secret}
      PMA_ABSOLUTE_URI: https://db.pokechess.local/
      UPLOAD_LIMIT: 64M
    depends_on:
      mariadb:
        condition: service_healthy
    networks:
      pokechess_net:
        ipv4_address: 10.1.0.6


  nginx:
    build:
      context: .
      dockerfile: docker/nginx/Dockerfile
      args:
        API_URL: ${API_URL:-/api}
    container_name: pokechess-nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./backend/public:/var/www/backend/public:ro
      - ./docker/nginx/ssl:/nginx/ssl:ro
    depends_on:
      php-fpm:
        condition: service_healthy
      phpmyadmin:
        condition: service_healthy
    networks:
      pokechess_net:
        ipv4_address: 10.1.0.3


networks:
  pokechess_net:
    driver: bridge
    ipam:
      driver: default
      config:
        - subnet: 10.1.0.0/24
          gateway: 10.1.0.1

volumes:
  mariadb_data:
